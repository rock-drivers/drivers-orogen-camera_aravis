# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(camera_aravisTaskLib)
ADD_LIBRARY(${CAMERA_ARAVIS_TASKLIB_NAME} SHARED 
    ${CAMERA_ARAVIS_TASKLIB_SOURCES})

add_dependencies(${CAMERA_ARAVIS_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${CAMERA_ARAVIS_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${CAMERA_ARAVIS_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${CAMERA_ARAVIS_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${CAMERA_ARAVIS_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${CAMERA_ARAVIS_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${CAMERA_ARAVIS_TASKLIB_HEADERS}
    DESTINATION include/orocos/camera_aravis)

